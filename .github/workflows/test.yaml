name: My Workflow
on:
  push:
    branches:
      - main
jobs:
  my-job:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v2
        # ปรับเป็นการอ้างอิงโค้ดจาก fork ของคุณเอง
      - name: Run tests
        run: |
          # รันคำสั่งทดสอบของคุณ
